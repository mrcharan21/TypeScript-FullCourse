A compiler is simply a translator.

It takes the code you wrote in one language (like TypeScript) and translates it into another language that a computer or browser can actually read and execute (like plain JavaScript).

In TypeScript, the compiler (tsc) has two main jobs:

Check your code for errors before it runs.
Translate your .ts files into standard .js files.