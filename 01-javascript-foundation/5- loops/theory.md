# Loops

Loops repeat code.

Common loops:

- `for` repeats a known number of times.
- `while` repeats while a condition is true.
- `for...of` loops over values in an iterable.
- `for...in` loops over object keys.
- Array methods like `forEach` can also repeat work.

Use the loop style that makes the intention clearest.

