# Async Await

`async` and `await` make Promise-based code easier to read.

An `async` function always returns a Promise. Inside it, `await` pauses execution until a Promise settles.

Use `try...catch` with `await` to handle errors clearly.

