# Data Types

JavaScript values have types. The main primitive types are `string`, `number`, `boolean`, `null`, `undefined`, `bigint`, and `symbol`.

Objects, arrays, and functions are non-primitive reference values.

Knowing the type of a value helps you choose the correct operation. For example, strings can be uppercased, numbers can be calculated, and booleans are useful for decisions.

