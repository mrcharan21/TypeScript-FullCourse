// Example 1: string
const userName = "Meera";
console.log(userName.toUpperCase());

// Example 2: number
const total = 150 + 75;
console.log(total);

// Example 3: boolean
const isLoggedIn = true;
console.log(isLoggedIn);

// Example 4: null and undefined
const selectedUser = null;
let nextPage;
console.log(selectedUser, nextPage);

// Example 5: object and array
const student = { name: "Ravi", score: 88 };
const subjects = ["JavaScript", "TypeScript"];
console.log(student, subjects);

// Example 6: product details use mixed data types
const productName = "Keyboard";
const productPrice = 1299;
const isAvailable = true;
console.log(productName, productPrice, isAvailable);

// Example 7: empty value for missing phone number
const phoneNumber = null;
console.log(`Phone number: ${phoneNumber}`);

// Example 8: undefined means value is not assigned yet
let deliveryDate;
console.log(`Delivery date: ${deliveryDate}`);

// Example 9: array stores a list of similar values
const dailyTemperatures = [31, 33, 30, 29];
console.log(dailyTemperatures);

// Example 10: object stores related information together
const bankAccount = {
  accountHolder: "Priya",
  balance: 5000,
  active: true,
};
console.log(bankAccount);
