# Conditions

Conditions let code make decisions.

Use `if`, `else if`, and `else` when checking different paths. Use `switch` when comparing one value against many fixed cases.

Conditions usually depend on boolean expressions such as comparisons, logical checks, or truthy and falsy values.

