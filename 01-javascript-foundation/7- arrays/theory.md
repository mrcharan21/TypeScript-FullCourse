# Arrays

Arrays store ordered lists of values.

You can access items by index, add or remove values, loop through arrays, and transform data with methods like `map`, `filter`, `reduce`, and `find`.

Arrays are one of the most common data structures in JavaScript.

