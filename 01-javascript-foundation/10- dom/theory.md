# DOM

The DOM, or Document Object Model, represents a web page as objects that JavaScript can read and change.

With DOM APIs, you can select elements, update text, change styles, add event listeners, create new elements, and respond to user actions.

DOM code runs in the browser, not directly in Node.js.

